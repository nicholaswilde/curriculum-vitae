# Specification - Add Emojis to README.md Title

## Overview
The goal of this track is to add `:briefcase:` and `:bust_in_silhouette:` emojis to either side of the main `# Curriculum Vitae` title in `README.md`.

## Requirements
- Update the title in `README.md` to `# :briefcase: Curriculum Vitae :bust_in_silhouette:`.
- Verify that `task lint` runs successfully.
